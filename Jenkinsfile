pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                // Checkout code from the repository
                echo 'building the application..'
            }
        }
    }
}
