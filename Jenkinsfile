pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                // Checkout code from the repository
                git 'https://github.com/your-repo/your-project.git'
            }
        }
    }
}
